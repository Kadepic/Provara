# ── Python ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# ── Cache colonnaire du lecteur (régénéré automatiquement) ─────────────
*.colf
.cache/
ia-lecteur/
.nonreg_cache.json

# ── Données volumineuses (NE PAS committer : reconstruites par ingere_*.py) ──
# L'échantillon dans datasets/lecteur/ EST versionné ; la base complète non.
# Les CONVERSATIONS de dev sont PRIVÉES : jamais committées, jamais embarquées dans le .exe.
datasets/conversations/
datasets/_raw/
datasets/**/*.dwarfs
# Gros fichiers de faits reconstruits localement (garder l'échantillon léger) :
datasets/lecteur/*.tsv

# ── OS / éditeurs ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
_REPRISE_SESSION.md

# ── Artefacts de build (.exe) ────────────────────────────────────────
VERSION_BUILD.txt
build/
dist/
*.spec
_POST_LINKEDIN.md

# carte des sujets : annexes AUTO (dérivables du store par outils/genere_sujets.py — frugalité : 11 Mo régénérables)
SUJETS_ANNEXES_AUTO.md
_REPRISE_MANDAT_SUJETS.md
